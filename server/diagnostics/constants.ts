/** README "Quick start": Node ≥ 20.11 — also `engines.node` in package.json. */
export const NODE_MIN = "20.11";
/** README "Quick start": herdr ≥ 0.7.1. There is deliberately no upper bound — see the design. */
export const HERDR_MIN = "0.7.1";
/**
 * README "Quick start": corral's `--name` / `--model` / `--remote-control` launch flags are verified
 * against this Claude Code build. Bump this and the README line together, in one PR.
 */
export const CLAUDE_VERIFIED = "2.1.232";
